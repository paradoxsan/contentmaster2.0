rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{userId}/{allPaths=**} {
      // Only authenticated users can read/write their own storage folder
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
